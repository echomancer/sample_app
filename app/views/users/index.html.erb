<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= page_entries_info @users %>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>
<%= page_entries_info @users %>
<%= will_paginate %>
